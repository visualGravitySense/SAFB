# Интеграция основного сайта с Vercel API

## ✅ Что настроено

Основной сайт (SAFunkBand) настроен для работы с Vercel API:
- ✅ Использует `VITE_API_URL` для подключения к API
- ✅ Автоматически определяет production/local окружение
- ✅ Имеет fallback на localStorage и default content

## 🔧 Настройка для Production

### Создайте файл `.env.production`

В папке `SAFunkBand/` создайте файл `.env.production`:

```env
VITE_API_URL=https://safb-admin.vercel.app/api
```

### Пересоберите сайт

```bash
cd SAFunkBand
npm run build
```

### Деплой

Задеплойте собранный сайт на GitHub Pages или другой хостинг.

## 🔧 Настройка для локальной разработки

### Создайте файл `.env.local`

В папке `SAFunkBand/` создайте файл `.env.local`:

```env
VITE_API_URL=http://localhost:3000/api
```

### Запустите локальный API

В другом терминале:
```bash
cd dashboard
npm run dev:api
```

### Запустите сайт

```bash
cd SAFunkBand
npm run dev
```

## 🔄 Как это работает

1. **Сайт загружается** → `ContentContext` загружает контент из API
2. **API запрос** → `GET https://safb-admin.vercel.app/api/content`
3. **API возвращает** → JSON с контентом
4. **Сайт отображает** → Контент из API

### Fallback механизм

Если API недоступен:
1. Пытается загрузить из localStorage (`saf_content`)
2. Если нет - использует default content
3. Сайт продолжает работать с дефолтным контентом

## 📝 Проверка работы

### 1. Проверьте API напрямую

```bash
curl https://safb-admin.vercel.app/api/content
```

Должен вернуться JSON с контентом.

### 2. Проверьте на сайте

1. Откройте сайт в браузере
2. Откройте DevTools → Network
3. Найдите запрос к `/api/content`
4. Проверьте, что данные загружаются

### 3. Проверьте обновления

1. Откройте Dashboard
2. Измените контент (например, заголовок Hero)
3. Сохраните
4. Обновите основной сайт
5. Изменения должны отобразиться

## ⚠️ Важно

### Хранение данных

Текущая реализация API использует временное хранилище `/tmp`, которое:
- ❌ Не сохраняет данные между перезапусками
- ❌ Файлы удаляются после завершения функции

**Для production рекомендуется:**
- Vercel Blob Storage для файлов
- База данных (MongoDB, Supabase) для контента

### CORS

CORS уже настроен в API, но если возникнут проблемы:
- Проверьте, что используете HTTPS URL
- Убедитесь, что домены разрешены в API

## 🔗 Связанные файлы

- `src/config/api.js` - Конфигурация API
- `src/services/contentApi.js` - Сервис для загрузки контента
- `src/context/ContentContext.jsx` - React Context для контента

## ✅ Готово!

После настройки `.env.production` и деплоя, сайт будет автоматически загружать контент из Vercel API.
